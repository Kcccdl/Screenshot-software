<Window x:Class="ScreenCapture.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="截图工具"
        Width="400" Height="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource WindowBgBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="&#x1F4F7;" FontSize="28" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="截图工具" FontSize="24" FontWeight="Bold"
                       Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 截图按钮 -->
        <Button Grid.Row="1" Click="ScreenshotButton_Click" Cursor="Hand" Margin="0,0,0,16"
                Style="{DynamicResource Win11AccentButton}" Padding="20,14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="&#x2702;" FontSize="20" Foreground="White"
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="开始截图" FontSize="16" FontWeight="SemiBold"
                           Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- 快捷键信息 -->
        <Border Grid.Row="2" Background="{DynamicResource CardBgBrush}" CornerRadius="6"
                Padding="12,10" Margin="0,0,0,16"
                BorderBrush="{DynamicResource DividerBrush}" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="快捷键:" Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="12" VerticalAlignment="Center"/>
                <TextBlock x:Name="HotkeyDisplayText" Text="PrintScreen"
                           Foreground="{DynamicResource AccentBrush}" FontSize="13" FontWeight="SemiBold"
                           VerticalAlignment="Center" Margin="8,0,0,0"/>
                <TextBlock Text="(可在设置中修改)" Foreground="{DynamicResource TextTertiaryBrush}"
                           FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- 操作说明 -->
        <StackPanel Grid.Row="3" VerticalAlignment="Center">
            <TextBlock Text="操作说明" Foreground="{DynamicResource TextTertiaryBrush}"
                       FontSize="11" Margin="0,0,0,8"/>
            <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" TextWrapping="Wrap">
                <Run Text="1. 按快捷键或点击按钮开始截图"/>
                <LineBreak/>
                <Run Text="2. 拖拽选择区域，或双击选中窗口"/>
                <LineBreak/>
                <Run Text="3. 在编辑器中添加标注"/>
                <LineBreak/>
                <Run Text="4. 复制到剪切板或另存为文件"/>
            </TextBlock>
        </StackPanel>

        <!-- 设置按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{DynamicResource Win11Button}"
                    Padding="14,8" Click="SettingsButton_Click">
                <TextBlock Text="⚙ 设置" FontSize="13" FontFamily="Segoe UI, Microsoft YaHei"/>
            </Button>
        </StackPanel>
    </Grid>
</Window>
